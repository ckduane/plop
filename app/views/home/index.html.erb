<div class="homepage">
  <div class="index-categories">
    <ul>
      <li>Workability</li>
      <li>Parking</li>
      <li>Wifi</li>
      <li>Seating</li>
      <li>Outlet</li>
      <li>Atmosphere</li>
    </ul>
  </div>

  <div class="index-sorts">
    <table>
    <% @r_workability.each do |restaurant| %>
      <tr>
        <td><h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3></td>
        <td class="score"><%= restaurant.work_score %></td>
      </tr>
      <tr>
        <td colspan="2"><%= restaurant.address %></td>
      </tr>
      <% end %>
    </table>

    <table>
    <% @r_parking.each do |restaurant| %>
      <tr>
        <td><h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3></td>
        <td class="score"><%= restaurant.score("parking_rating") %></td>
      </tr>
      <tr>
        <td colspan="2"><%= restaurant.address %></td>
      </tr>
      <% end %>
    </table>

    <table>
    <% @r_wifi.each do |restaurant| %>
      <tr>
        <td><h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3></td>
        <td class="score"><%= restaurant.score("wifi_rating") %></td>
      </tr>
      <tr>
        <td colspan="2"><%= restaurant.address %></td>
      </tr>
      <% end %>
    </table>

    <table>
    <% @r_seating.each do |restaurant| %>
      <tr>
        <td><h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3></td>
        <td class="score"><%= restaurant.score("seating_rating") %></td>
      </tr>
      <tr>
        <td colspan="2"><%= restaurant.address %></td>
      </tr>
      <% end %>
    </table>

    <table>
    <% @r_outlet.each do |restaurant| %>
      <tr>
        <td><h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3></td>
        <td class="score"><%= restaurant.score("outlet_rating") %></td>
      </tr>
      <tr>
        <td colspan="2"><%= restaurant.address %></td>
      </tr>
      <% end %>
    </table>

    <table>
    <% @r_atmosphere.each do |restaurant| %>
      <tr>
        <td><h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3></td>
        <td class="score"><%= restaurant.score("atmosphere_rating") %></td>
      </tr>
      <tr>
        <td colspan="2"><%= restaurant.address %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
